lexik_jwt_authentication:
    secret_key:       '%kernel.project_dir%/config/jwt/private-test.pem' # required for token creation
    public_key:       '%kernel.project_dir%/config/jwt/public-test.pem'  # required for token verification
    pass_phrase:      'test' # required for token creation, usage of an environment variable is recommended
    token_ttl:        3600
